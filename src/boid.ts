export{};
function returnNothing(): void{
	console.log("I don't reeturn anyting")
	// return undefind;
}

console.log(returnNothing());

//概念のお話
//void型は関数が何も返さないことを示す型
//undefindは関数が明示的に何も返さない、（戻り値がない）ことを意味する
//ただvoid = undefind ではない。